import { DbService } from './db.service';
import { HttpClient } from 'selenium-webdriver/http';
const urlService: string = "http://daas.marconirovereto.it:50055/";

var json: string;

export class Ricerche {
    public getFamiglia(famiglia: string) {
        
    }
}
